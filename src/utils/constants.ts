export enum TypeButtonEnum {
  PRIMARY = "primary",
  NORMAL = "normal",
  GRAY = "gray",
}

export enum SelectedSidebar {
  USER_INFO = "user-info",
  PRODUCT_INFO = "product-info",
  LOGOUT = "logout",
}
